# Nags-Vet

> Сайт ветеринарной клиники Nags-Vet — Chișinău, Moldova

Современный многоязычный лендинг ветеринарной клиники с поддержкой румынского, русского, английского и украинского языков.

## 🛠 Технологии

- **Next.js 16** — App Router
- **React 19**
- **next-intl** — интернационализация (ro, ru, en, ua)
- **Tailwind CSS 4**
- **Framer Motion** — анимации
- **Lucide React** — иконки

## 📦 Установка

```bash
# Установка зависимостей
pnpm install

# Разработка
pnpm dev

# Сборка
pnpm build

# Продакшен
pnpm start

# Линтинг
pnpm lint
```

## ⚙️ Переменные окружения

Создайте `.env.local`:

```env
NEXT_PUBLIC_SITE_URL=https://your-domain.com
```

Используется для корректной генерации `sitemap.xml` и `robots.txt`.

## 📁 Структура

```
src/
├── app/           # App Router, layout, sitemap, robots
├── config/        # Конфигурация сайта
├── i18n/          # Настройки локализации
├── shared/        # UI компоненты
└── widgets/       # Секции страницы
```

## 🌐 SEO

- **Sitemap** — `/sitemap.xml` с поддержкой hreflang для 4 языков
- **Robots** — `/robots.txt` разрешает индексацию

## 📄 Лицензия

Приватный проект.
